#include <stdio.h>
int main()
{
    int n1 = 5;
    int n2 = 10;
    
    // logical and(&&)

    printf("%d\n", (n1 == 5) && (n2 > 20));

    // logical or(||)

    printf("%d\n",(n1 == 100) || (n2 > 20));

    // logical not(!)
}